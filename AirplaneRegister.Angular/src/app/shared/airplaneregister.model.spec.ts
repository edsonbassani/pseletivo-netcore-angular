import { Airplaneregister } from './airplaneregister.model';

describe('Airplaneregister', () => {
  it('should create an instance', () => {
    expect(new Airplaneregister()).toBeTruthy();
  });
});
